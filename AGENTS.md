# Инструкции для ИИ-ассистента

## Контекст проекта
Мы разрабатываем архитектуру и код WFX-плагина для Total Commander — "Telegram VFS".
Этот плагин создает и монтирует приватные Telegram-каналы как виртуальную файловую систему, используя C#, WTelegramClient, Native AOT и SQLite в качестве локального кэша иерархии папок. Плагин тестируется на Total Commander 32 bit v7.50a.

## Главное правило (Автоматическое ведение документации)
Единым источником истины по архитектуре, структуре БД и логике работы проекта является файл `README.md`.

**Твои обязательные действия:**
1. **Помни архитектуру:** Опирайся на концепции, описанные в `README.md` при любых обсуждениях кода или функционала.
2. **Авто-обновление:** Если в ходе диалога пользователь предлагает новую идею, меняет структуру базы данных, добавляет фичу (например, новый механизм кэширования или шифрования) и вы приходите к согласию — **ТЫ ОБЯЗАНА автоматически внести эти изменения в `README.md`**. Не жди, пока пользователь попросит обновить документацию. Делай это сама в том же ответе, где вы утвердили изменение.
3. Проверяй `README.md` (через `view_file`), если сомневаешься в текущей структуре перед тем, как предлагать новые решения.
4. **Ведение плана:** Актуальный план задач и статус их выполнения хранится в файле `ROADMAP.md`. При завершении логического этапа обновляй этот файл (ставь галочки `[x]` и кратко описывай результат).

## Компиляция и проверка кода
В среде должен быть установлен компилятор .NET 9.0 SDK. Ты ОБЯЗАНА проверять любой написанный или изменённый C# код на компилируемость перед тем, как давать ответ. 
Путь к компилятору: `/workspace/.dotnet/dotnet`
Команда для установки (если бинарник отсутствует в новом чате/сессии):
`mkdir -p /workspace/.dotnet && curl -sSL https://dotnetcli.azureedge.net/dotnet/Sdk/9.0.100/dotnet-sdk-9.0.100-linux-x64.tar.gz | tar -xz -C /workspace/.dotnet`
Команда для сборки проекта: `/workspace/.dotnet/dotnet build src/TgVfsPlugin/TgVfsPlugin.csproj -p:EnableWindowsTargeting=true`
Если при первом запуске бинарник `/workspace/.dotnet/dotnet` не найден — автоматически выполни команду установки, а затем обязательно проверь сборку проекта перед ответом.
Никогда не утверждай, что код работает, пока не проверишь его этой командой!

## Стандарты UI интерфейса (Обязательно к исполнению)
При создании любых новых диалогов, окон или контролов ты ОБЯЗАНА использовать фабричные методы `UiTheme`:
1. **Высота элемента:** Все кнопки (`Button`), поля ввода (`TextBox`) и выпадающие списки (`ComboBox`) ДОЛЖНЫ иметь строгую фиксированную высоту **30 px** (`UiTheme.DefaultButtonHeight` / `UiTheme.DefaultInputHeight`). Текстовые метки (`Label`) ДОЛЖНЫ иметь высоту не менее **22 px** (`UiTheme.DefaultLabelHeight`), чтобы не срезались нижние хвосты букв («у», «д», «р», «ю»).
2. **Фабричные методы:**
   - Создание кнопок: `UiTheme.CreateButton(...)`
   - Создание полей ввода: `UiTheme.CreateTextBox(...)`
   - Создание выпадающих списков: `UiTheme.CreateComboBox(...)`
   - Создание текстовых меток: `UiTheme.CreateLabel(...)`
   - Создание формы: `UiTheme.CreateDialogForm(...)`
   - Создание нижней панели: `UiTheme.CreateBottomPanel(52)`
3. **Геометрия и сетка:**
   - Отступы по бокам (`margin`): **20 px**.
   - Отступ сверху (`topMargin`): **16 px**.
   - Отступ снизу от последнего элемента до нижней панели: **16 px**.
   - Высота нижней панели с кнопками: **52 px** (кнопки размещаются по центру панели, `Top = 11`).
4. **Центрирование текста:** Для кнопок обязательно `TextAlign = ContentAlignment.MiddleCenter` и `ImageAlign = ContentAlignment.MiddleCenter`.
5. **Масштабирование:** Каждая форма обязана иметь `AutoScaleMode = AutoScaleMode.None`.
Запрещено хардкодить произвольную высоту кнопок или текстовых полей вне `UiTheme`!
